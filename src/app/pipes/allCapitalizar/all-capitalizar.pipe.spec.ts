import { AllCapitalizarPipe } from './all-capitalizar.pipe';

describe('AllCapitalizarPipe', () => {
  it('create an instance', () => {
    const pipe = new AllCapitalizarPipe();
    expect(pipe).toBeTruthy();
  });
});
